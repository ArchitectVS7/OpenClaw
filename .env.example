# OpenClaw Docker Environment Variables
# Copy this file to .env and fill in your values
# DO NOT commit .env to git (it's already in .gitignore)

# Gateway Configuration
OPENCLAW_CONFIG_DIR=/path/to/.openclaw
OPENCLAW_WORKSPACE_DIR=/path/to/.openclaw/workspace
OPENCLAW_GATEWAY_PORT=18789
OPENCLAW_BRIDGE_PORT=18790
OPENCLAW_GATEWAY_BIND=lan
OPENCLAW_GATEWAY_TOKEN=

# AI Provider Keys (optional - can also be configured via CLI)
CLAUDE_AI_SESSION_KEY=
CLAUDE_WEB_SESSION_KEY=
CLAUDE_WEB_COOKIE=

# Docker Build Options
OPENCLAW_IMAGE=openclaw:local
OPENCLAW_DOCKER_APT_PACKAGES=
OPENCLAW_EXTRA_MOUNTS=
OPENCLAW_HOME_VOLUME=

# Note: docker-setup.sh will auto-generate most of these values
# Run: ./docker-setup.sh to initialize
